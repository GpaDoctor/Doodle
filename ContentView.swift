import SwiftUI

struct ContentView: View {
    var body: some View {
        NavigationStack {
           NavigationLink( destination:
             TimetableView(){
              Image("timetable")
                 .resizable(resizingMode: .stretch)
                 .aspect
        
              